{{/* T3 Foundation Gemstone Project https://t3gemstone.org */}}
{{/* SPDX-License-Identifier Apache-2.0 */}}

{{/* https://github.com/go-debos/debos */}}
{{/* https://github.com/go-debos/debos-recipes */}}
{{/* https://pkg.go.dev/github.com/go-debos/debos/actions */}}
{{/* https://golang.org/pkg/text/template */}}

{{- $arch      := or .arch      "arm64" -}}
{{- $machine   := or .machine   "t3-gem-o1" -}}
{{- $base      := or .base      "ubuntu" -}}
{{- $type      := or .type      "minimal" -}}
{{- $suite     := or .suite     "jammy" -}}
{{- $release   := or .release   "v2026.2" -}}
{{- $workdir   := or .workdir   "/home/workdir" -}}
{{- $image     := or .image     (printf "gemstone-%s-%s-%s-%s-%s.img" $type $release $base $suite $machine) -}}
{{- $imagepath := or .imagepath (printf "%s/build/debos/%s" $workdir $image) -}}
{{- $imagesize := or .imagesize "4096M" -}}
{{- $ci        := or .ci        "false" -}}
{{- $encrypt   := or .encrypt   "false" -}}
{{- $mobian    := or .mobian    "false" -}}

architecture: {{ $arch }}

actions:

  - action: debootstrap
    suite: {{ $suite }}
    variant: minbase
    keyring-file: /usr/share/keyrings/{{ $base }}-archive-keyring.gpg
{{ if eq $base "debian" }}
    components: [ main, contrib ]
    mirror: https://deb.debian.org/debian
{{ else if eq $base "pardus" }}
    components: [ main, non-free, contrib ]
    mirror: https://depo.pardus.org.tr/pardus
    suite: {{ $suite }}-deb
{{ else }}
    components: [ main, multiverse, universe ]
    mirror: {{ if eq $arch "amd64" }}'https://archive.ubuntu.com/ubuntu'{{ else }}'https://ports.ubuntu.com/ubuntu-ports'{{ end }}
{{ end }}
    exclude:
    - systemd-zram-generator
    - phosh-tour
    - build-essential

{{ if eq $base "pardus" }}

  - action: download
    url: {{ if eq $suite "yirmibes" }}'https://depo.pardus.org.tr/pardus/pool/main/p/pardus-archive-keyring/pardus-archive-keyring_2025.1_all.deb'{{ else }}'https://depo.pardus.org.tr/pardus/pool/main/p/pardus-archive-keyring/pardus-archive-keyring_2021.1_all.deb'{{ end }}
    name: pardus_archive_keyring
    filename: pardus_archive_keyring.deb

  - action: overlay
    description: Copy downloaded Pardus archive keyring
    origin: pardus_archive_keyring
    destination: /pardus_archive_keyring.deb

  - action: run
    chroot: true
    command: |
      DEBIAN_FRONTEND=noninteractive dpkg -i /pardus_archive_keyring.deb
      rm -f /pardus_archive_keyring.deb

  - action: run
    chroot: true
    script: scripts/all/setup-pardus.sh {{ $suite }}

{{ end }}

  - action: run
    description: Speed up dpkg/apt under QEMU emulation
    chroot: true
    command: |
      echo 'force-unsafe-io' > /etc/dpkg/dpkg.cfg.d/00-unsafe-io
      echo 'man-db man-db/auto-update boolean false' | debconf-set-selections
      echo 'APT::Acquire::Queue-Mode "host";'        > /etc/apt/apt.conf.d/00parallel
      echo 'APT::Acquire::http::Pipeline-Depth 5;'  >> /etc/apt/apt.conf.d/00parallel

  - action: apt
    recommends: false
    packages: [ apt-utils, eatmydata ]

  - action: run
    description: Configure eatmydata for subsequent apt/dpkg calls
    chroot: true
    command: |
      echo 'DPkg::Pre-Invoke  { "export LD_PRELOAD=/usr/lib/libeatmydata/libeatmydata.so"; };' > /etc/apt/apt.conf.d/00eatmydata
      echo 'DPkg::Post-Invoke { "unset LD_PRELOAD || true"; };' >> /etc/apt/apt.conf.d/00eatmydata

  - action: recipe
    description: Install common base packages
    recipe: distro-base.yaml
    variables:
      arch: {{ $arch }}
      suite: {{ $suite }}

{{ if eq $type "kiosk" }}
  - action: recipe
    description: Install kiosk packages
    recipe: distro-kiosk.yaml
    variables:
      arch: {{ $arch }}
{{ end }}

{{ if eq $type "desktop" }}
  - action: recipe
    description: Install desktop packages
    recipe: distro-desktop.yaml
    variables:
      arch: {{ $arch }}
      suite: {{ $suite }}
      machine: {{ $machine }}
      type: {{ $type }}
      phase: packages
{{ end }}

{{ if eq $type "tablet" }}
  - action: recipe
    description: Install tablet packages
    recipe: distro-tablet.yaml
    variables:
      arch: {{ $arch }}
      base: {{ $base }}
      suite: {{ $suite }}
      machine: {{ $machine }}
      imagepath: {{ $imagepath }}
      phase: packages
{{ end }}

  - action: run
    description: Setup host
    chroot: true
    command: |
      echo "{{ $machine }}" >/etc/hostname
      echo "127.0.0.1	localhost {{ $machine }}" >/etc/hosts

  - action: run
    description: Change distro issue message
    chroot: true
    command: |
      MSG="Gemstone {{ $type }}:{{ $release }}"
      echo "$MSG" >/etc/issue.net
      echo "$MSG\n" >/etc/issue

{{ if eq $type "desktop" }}
  - action: recipe
    description: Seed desktop /etc/skel before adduser
    recipe: distro-desktop.yaml
    variables:
      arch: {{ $arch }}
      type: {{ $type }}
      phase: skel
{{ end }}

  - action: run
    chroot: true
    script: scripts/all/setup-user.sh {{ $type }}

  - action: overlay
    source: overlays/minimal/all
    destination: /

  - action: overlay
    source: overlays/minimal/{{ $machine }}
    destination: /

  - action: run
    chroot: true
    script: scripts/{{ $machine }}/install-local-apt.sh {{ $machine }} {{ $type }} {{ $ci }}

{{ if eq $type "desktop" }}
  - action: recipe
    description: Install t3-gem-vnc
    recipe: distro-desktop.yaml
    variables:
      arch: {{ $arch }}
      machine: {{ $machine }}
      phase: vnc
{{ end }}

{{ if eq $ci "false" }}
  - action: run
    chroot: false
    script: scripts/{{ $machine }}/install-bootfiles.sh {{ $workdir }} $ROOTDIR
{{ end }}

{{ if eq $machine "beagley-ai" }}
  - action: run
    chroot: true
    script: scripts/beagley-ai/setup-bootfiles.sh

  - action: run
    chroot: false
    script: scripts/beagley-ai/install-firmware.sh {{ $workdir }} $ROOTDIR
{{ end }}

{{ if eq $machine "t3-gem-o1" }}
  - action: run
    chroot: true
    script: scripts/t3-gem-o1/setup-bootfiles.sh

  - action: run
    chroot: false
    script: scripts/t3-gem-o1/install-firmware.sh {{ $workdir }} $ROOTDIR
{{ end }}

{{ if eq $type "kiosk" "desktop" "tablet" }}
  - action: overlay
    source: overlays/{{ $type }}/all
    destination: /

  - action: overlay
    source: overlays/{{ $type }}/{{ $machine }}
    destination: /
{{ end }}

{{ if eq $type "desktop" }}
  - action: recipe
    description: Desktop post-install (firefox, vscode, icon cache)
    recipe: distro-desktop.yaml
    variables:
      arch: {{ $arch }}
      phase: postinstall
{{ end }}

{{ if eq $machine "beagley-ai" "t3-gem-o1" }}
  - action: overlay
    source: overlays/minimal/usb-gadgets
    destination: /
{{ end }}

{{ if eq $type "tablet" }}
  - action: recipe
    description: Tablet post-install (selinux, flatpak, credentials, services)
    recipe: distro-tablet.yaml
    variables:
      arch: {{ $arch }}
      workdir: {{ $workdir }}
      image: {{ $image }}
      imagepath: {{ $imagepath }}
      phase: postinstall
{{ end }}

  - action: run
    chroot: true
    script: scripts/all/setup-permissions.sh {{ $type }}

  - action: run
    chroot: true
    script: scripts/all/setup-networking.sh {{ $machine }} {{ $suite }}

{{ if ne $machine "qemuarm64" }}
  - action: run
    chroot: true
    script: scripts/all/setup-gemstone-apt.sh {{ $machine }} {{ $suite }}
{{ end }}

  - action: run
    chroot: true
    script: scripts/all/setup-services.sh

  - action: run
    chroot: true
    script: scripts/{{ $machine }}/setup-services.sh

  - action: run
    chroot: true
    script: scripts/all/disable-services.sh {{ $type }} {{ $machine }}

  - action: run
    chroot: true
    script: scripts/{{ $machine }}/clean-up.sh {{ $type }}

{{ if eq $type "tablet" }}
  - action: recipe
    description: Tablet finalize (dconf update)
    recipe: distro-tablet.yaml
    variables:
      arch: {{ $arch }}
      imagepath: {{ $imagepath }}
      phase: finalize
{{ end }}

  - action: run
    chroot: true
    script: scripts/all/clean-up.sh {{ $type }} {{ $base }}

  - action: run
    chroot: true
    script: scripts/all/setup-info.sh {{ $type }} {{ $release }}

  - action: image-partition
    imagename: {{ $image }}
    imagesize: {{ $imagesize }}
{{ if eq $machine "beagley-ai" "t3-gem-o1" "qemuarm64" }}
    partitiontype: msdos
    mountpoints:
      - mountpoint: /
        partition: root
      - mountpoint: /boot
        partition: BOOT
    partitions:
      - name: BOOT
        fs: vfat
        start: 1MiB
        end: 129MiB
        extendedoptions: [-g, 16/32, -h, 0]
        flags: [boot, lba]
        parttype: 0C
      - name: root
        fs: btrfs
        start: 129MiB
        end: 100%
        parttype: 83
{{ else if eq $arch "amd64" }}
    partitiontype: gpt
    partitions:
      - name: efi
        fs: vfat
        start: 0%
        end: 50MB
        options: [ x-systemd.automount ]
      - name: root
        fs: ext4
        start: 50MB
        end: 100%
    mountpoints:
      - mountpoint: /
        partition: root
      - mountpoint: /boot/efi
        partition: efi
        flags: [ boot ]
{{ end }}

  - action: filesystem-deploy
    description: Deploying filesystem onto image
